{% extends "layout.njk" %}

{% block content %}

<div class="card table-container">
  <a href="students/create">Novo</a>
  <table width="100%">
    <thead>
      <tr>
        <th>Nome Completo</th>
        <th>E-mail</th>
        <th>Ano Escolar</th>
        <th>Ação</th>
      </tr>
    </thead>

    <tbody>
      {% for student in students %}
        <tr>
          <td>
            <span id="profile-image" style="background-image: url({{ student.avatar }})"></span>
            {{ student.name }}
          </td>
          <td>{{ student.email }}</td>
          <td>
            {% if student.schoolYear == "5.EF" %}
              5º ano do ensino fundamental
            {% endif %}
            {% if student.schoolYear == "6.EF" %}
              6º ano do ensino fundamental
            {% endif %}
            {% if student.schoolYear == "7.EF" %}
              7º ano do ensino fundamental
            {% endif %}
            {% if student.schoolYear == "8.EF" %}
              8º ano do ensino fundamental
            {% endif %}
            {% if student.schoolYear == "9.EF" %}
              9º ano do ensino fundamental
            {% endif %}
            {% if student.schoolYear == "1.EM" %}
              1º ano do ensino médio
            {% endif %}
            {% if student.schoolYear == "2.EM" %}
              2º ano do ensino médio
            {% endif %}
            {% if student.schoolYear == "3.EM" %}
              3º ano do ensino médio
            {% endif %}
          </td>
          <td><a href="/students/{{ student.id }}">Ver</a></td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
</div>

{% endblock content %}